[build]
  base = "frontend"
  command = "npm install --no-audit --no-fund && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "https://valuationdcf-api.fly.dev/api/:splat"
  status = 200
  force = true
